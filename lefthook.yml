pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts?}'
      run: npx eslint {staged_files}
    types:
      glob: '*.{ts?}'
      run: npx tsc --noEmit
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
