name: Call a reusable workflow

on:
    push:
      branches: [development]
    pull_request:
      branches: [development]
jobs:
  eslint:
    uses: Globy-Solutions/workflows-actions/eslint.yml@master

  prettier:
    uses: Globy-Solutions/workflows-actions/prettier.yml@master

  jest:
    uses: Globy-Solutions/workflows-actions/jest.yml@master
