name: Call a reusable workflow

on:
    push:
      branches: [development]
    pull_request:
      branches: [development]
jobs:
  eslint:
    uses: Globy-Solutions/workflows-actions/eslint.yml@main

  prettier:
    uses: Globy-Solutions/workflows-actions/prettier.yml@main

  jest:
    uses: Globy-Solutions/workflows-actions/jest.yml@main
